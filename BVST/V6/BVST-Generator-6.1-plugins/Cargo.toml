[workspace]
members = [
    "System/bvst_lib",
    "Synths/*/audio_engine",
    "Plugins/*/*/audio_engine"
]
resolver = "2"

# Optimize for size for all members when compiling in release mode
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
