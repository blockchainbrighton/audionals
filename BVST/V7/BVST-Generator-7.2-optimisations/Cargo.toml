[workspace]
members = [
    "System/bvst_lib",
    # "Plugins/*/*/audio_engine" # Uncomment when plugins are added
]
resolver = "2"

# Optimize for size for all members when compiling in release mode
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true