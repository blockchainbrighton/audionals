{
  "name": "ordspd-v2026",
  "version": "1.0.0",
  "description": "Automated tests for ordSPD-v2026",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "measure:load": "node scripts/measure-load.mjs",
    "measure:fast": "MEASURE_TARGET_DECODE_COUNT=1 node scripts/measure-load.mjs"
  },
  "type": "module",
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "jsdom": "^28.0.0",
    "playwright-core": "^1.58.2",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
